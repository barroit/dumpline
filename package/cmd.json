dnl SPDX-License-Identifier: GPL-3.0-or-later
dnl
{
   "command": "dumpline.multiple",
   "title": "copy and preview result",
   "category": "dumpline",
   "enablement": "editorHasSelection && !editorHasMultipleSelections"
},
{
   "command": "dumpline.single",
   "title": "copy and preview result (for single line)",
   "category": "dumpline",
   "enablement": "editorHasSelection && !editorHasMultipleSelections"
}
